# Example environment variables for the backend
MONGODB_URI=mongodb+srv://username:password@cluster0.example.mongodb.net/northstar?retryWrites=true&w=majority
DB_NAME=northstar
PORT=3000
# Comma-separated origins allowed to call the API (no trailing slash).
FRONTEND_URL=http://localhost:5173,https://northstar-support-phi.vercel.app
NODE_ENV=development
